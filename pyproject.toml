
[project]
dependencies = [
    "Django<5.0,>=4.2",
    "mypy>=1.18.2",
    "ruff>=0.14.2",
    "bandit>=1.8.6",
    "pytest>=8.4.2",
]
requires-python = ">=3.13"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
